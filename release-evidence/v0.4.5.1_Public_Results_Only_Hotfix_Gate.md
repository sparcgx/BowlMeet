# v0.4.5.1｜Public Results Only Hotfix Gate

## Decision

**PROMOTION READY — 29 PASS / 0 FAIL**

- Stable source: `v0.4.5` / merge `057a3aa4109f1efd8054c124805a1e0a96913d7d`
- Hotfix version: `v0.4.5.1`
- Scope: leaderboard and every score-share card use published PUBLIC results only
- P0 / P1 / P2 blockers: `0 / 0 / 0`
- Supabase migration: **NONE**
- PUBLIC payload write during release: **NONE**

Pre-promotion read-only PUBLIC snapshot: revision `24`, `3` meetups, `2` sessions, and `2` record controls. Promotion must leave these values unchanged.

## Corrected Behavior

1. Leaderboard all-history aggregation uses only published remote sessions.
2. Single-meetup leaderboard uses only that meetup's published remote session.
3. Leaderboard player/game/average/best KPIs use the same public-only source.
4. Leaderboard and Share selectors list only meetups linked to a published session.
5. Ranking, full-score, summary, and award share cards use public-only data.
6. Local-only and pending-sync sessions are excluded.
7. Sessions with a `removed` public record control are excluded.
8. Live and post-review share entry points block unpublished meetups and request public sync first.
9. The editable Awards view remains local; only the generated shared award card is public-only.

## Regression Safety

- One existing PUBLIC history source is reused; no second score store was created.
- Session, Backup, Roster, Personal Data, tombstone, Draft, Settings, and Live State formats are unchanged.
- `PUBLIC` Cloud code and production local-storage namespaces are unchanged.
- Supabase schema and production patch blobs remain byte-identical to `v0.4.5`.
- Frozen `stable/v0.4.5` and `v0.4.5-RC.1` are not modified.

## Automated Gate

Run:

```bash
node tools/v0.4.5.1-public-results-gate.mjs
```

Result: **29 PASS / 0 FAIL**

The gate checks JavaScript, DOM IDs/references, manifest, Service Worker, version/cache metadata, PUBLIC source helpers, leaderboard and share source isolation, entry guards, tombstone exclusion, fixture behavior, production SQL blob integrity, and Git diff integrity.

## Artifact Integrity

| Artifact | SHA-256 |
| --- | --- |
| `index.html` | `aa58df6e663def2474cfc7f5999ac015f6dde8dc58ab466d912bfe7a828b0de5` |
| `manifest.webmanifest` | `8442cf8c6cc8013017330bf08bcc1bbebce8e8e9791387dc417cbc5f18311f79` |
| `sw.js` | `2f7dddd42ad3f3d15ef1e701d89d55c753dce0d3915d26135bbf9b0e75a85546` |

## Promotion Rule

Promote through `hotfix/v0.4.5.1-public-results-only` → PR → `main`, then create `stable/v0.4.5.1` from the exact merge commit. Post-promotion verification must confirm the PUBLIC revision and counts are unchanged.
